function tong(x, y) {
    return console.log("Tong 2 so: " + (x + y));
}

function hieu(x, y) {
    return console.log("Hieu 2 so: " + (x - y));
}

function tich(x, y) {
    return console.log("Tich 2 so: " + (x * y));
}

function thuong(x, y) {
    return console.log("Thuong 2 so: " + (x / y));
}

module.exports = { tong, hieu, tich, thuong }

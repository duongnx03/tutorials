<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <title>List</title>
    <style>
        th{
            margin-left: 30px;
            margin-right: 30px;
        }
    </style>
</head>
<body>
    <div class="container">
        <%if(user){%>
            <h5 class="text-success">Welcome: <%=user.name%></h5>
        <%}%>
        <h1>User List</h1>
        <%if(message){%> 
           <div class="alert alert-success alert-dismissable">
            <button class="btn btn-close" data-bs-dismiss="alert">                  
            </button>
            <strong>success!</strong>
            <%= message%>
           </div> 
        <%}%>
        <a href="/user/create" class="btn btn-info">Create</a><br><br>
        <table class="table table-hover">
            <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Age</th>
                <th>Role</th>
                <th>Action</th>
            </tr>
            <% users.forEach(item => {%>          
            <tr>
                <td><%=item.name%></td>
                <td><%=item.email%></td>
                <td><%=item.age%></td>
                <td><%=item.role%></td>   
                <td>
                    <a href="/user/detail" class="btn btn-success">Detail</a> <!-- Nút cập nhật -->
                    <a href="/user/update/<%=item._id%>" class="btn btn-primary">Update</a> <!-- Nút cập nhật -->
                    <a href="/user/delete/<%=item._id%>" class="btn btn-danger">Delete</a> <!-- Nút xóa -->
                </td>
            </tr>              
            <%})%>
        </table>
    </div>
</body>
</html>